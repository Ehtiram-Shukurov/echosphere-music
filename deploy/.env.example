# Copy to .env (never commit .env). setup-vm.sh creates one for you.
ECHOSPHERE_HOSTNAME=your-name.duckdns.org
ECHOSPHERE_API_KEY=replace-with-a-long-random-value
ECHOSPHERE_RETENTION_HOURS=24
ECHOSPHERE_MAX_STORAGE_GB=20
